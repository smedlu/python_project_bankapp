[tool.poetry]
name = "pythonproject2"
version = "0.1.0"
description = ""
authors = ["smedlu <smedlu@example.com>"]

[tool.poetry.dependencies]
python = "^3.12"
pandas = "^2.0.0"
openpyxl = "^3.1.0"
requests = "^2.31.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"
mypy = "^1.5.0"
flake8 = "^6.0.0"
isort = "^5.12.0"
pandas-stubs = "^2.0.0"
types-requests = "^2.31.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
